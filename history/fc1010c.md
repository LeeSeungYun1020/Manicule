# fc1010c — refactor: 바코드 리더가 필요할 때만 동작하도록 변경

**Files**: `BarcodeReader.kt`, `DemandDrivenBarcodeReader.kt`, `MlKitBarcodeReaderFactory.kt`

## 결정
- `BarcodeReader`가 `ImageAnalysis`를 소유하고 `getBarcodes()` 대기자 수로 analyzer 실행을 제어한다.
- 동시 호출은 replay 없는 공유 감지 흐름에서 같은 프레임을 각 predicate로 평가한다.

## 이유
- 분석 수명을 화면이나 명시적 Session이 아니라 실제 응답 수요와 일치시킨다.
- `WhileSubscribed`로 마지막 호출의 성공·실패·취소에서 중단하고 다음 호출에서 재시작한다.
