# 3632472 — fix: 타입 안전 네비게이션 적용

**Files**: `navigation/{TopLevelDestination,ManiculeAppState,ManiculeNavHost}.kt`, `app/build.gradle.kts`

## 결정
String route 대신 `@Serializable` 기반 type-safe route (Navigation 2.8+).

## 이유
- 컴파일타임에 route / argument 타입 검증 → 런타임 크래시 가능성 제거.
- `kotlinx.serialization` 은 NLK API DTO 용으로 어차피 필요 → 추가 비용 0.
- Deep link / external URI 는 별도 string mapping 필요 (3+ 단계 통합 시 점검).
