# 5053f6f — docs: Navigation 연결 책임과 레인 완료 기준 명문화

**Files**: `plan/order.md`, `plan/structure.md`, `skills/work.md`

## 결정
- Navigation을 Destination-local(V 레인 완료 기준, 최소 app 연결 포함)과 Cross-destination(I1 점진 조립)으로 분리하고 production 기본 빈 람다 금지.

## 이유
- feature의 `NavController` 비의존 원칙을 유지하면서 I1 지연으로 인한 no-op 버튼 노출 및 조립 누락 방지.
- 대안 검토:
  - feature가 `NavController` 직접 조작: 공식 권장 아키텍처 위배 및 결합도 증가로 기각.
  - I1 일괄 조립 유지: 화면은 노출되었으나 버튼이 동작하지 않는 결함 발생으로 기각.

## 기존 코드 감사 및 후속 이슈 후보
- **V2 Scanner**: `ScannerNavigation.kt` 기본 빈 람다 제거 및 `ManiculeNavHost` 내 `onNavigateToSearch` 연결 필요.
- **V5 Library**: V2 미완성 상태에서 `LibraryScreen` 빈 상태의 스캔 진입 활성화 여부 정책 확정 필요.
