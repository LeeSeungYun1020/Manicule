# 문서 인덱스

작업에 필요한 문서 제목을 검색하여 관련 부분만 읽는다.
계획 문서 수정 시 절 번호·앵커로 연결하지 않고, 변경은 담당 문서에서만 한다.

| 찾는 정보 | 원본·읽기 경로 |
|---|---|
| 사용자 행동·상태·예외·화면 이동 | [기획](plan/plan.md)의 해당 기능 |
| 아키텍처·의존 방향·공용 경계·주요 파일 | [구조](plan/structure.md)의 관련 모듈 |
| 선행 조건·변경 소유권·PR 분할 | [순서](plan/order.md)의 공통 규칙과 해당 레인 |
| 완료 현황·다음 범위 | [현황](plan/current.md)의 해당 레인. 상태나 다음 범위가 바뀔 때만 갱신 |
| UI 구현·확정 편차 | [UI 가이드](plan/prototype/ui-ux-guidelines.md)에서 공통 규칙·완료 점검을 읽고 해당 화면의 컴포넌트 지도·프로토타입으로 이동 |
| 계획·구현·리뷰·반영·검수 절차 | [작업 절차](skills/skills.md)의 요청 단계와 그 진입점 |
| 과거 결정의 이유 | [history](history/README.md)의 관련 기록. 새 결정은 작업 절차에서 필요할 때만 기록 |

# 선택 읽기

- 계획은 최신 `origin/main`의 `plan/current.md` 해당 레인, `plan/order.md`의 공통 규칙·해당 레인, 관련 요구사항·모듈 구조와 기존 코드를 확인. 의사결정을 확정한 `plan/tasks/<작업>.md`만 추가한다.
- 구현은 작업 계획 파일과 연결된 요구사항·관련 코드를 우선 읽는다.
- 리뷰는 PR base의 `plan/` 관련 문서에서 요구사항·경계를 독립적으로 확인하고 PR의 범위·설계·코드·테스트 및 계획 문서 변경과 대조한다. UI 변경은 UI 가이드와 프로토타입을 확인한다. 계획 단계에서 생성한 `plan/tasks/<작업>.md`는 리뷰 입력으로 요구하지 않는다.
- 검수는 PR 설명과 프로젝트 계획 문서 관련 내용, 코드, 테스트를 확인한다. 같은 세션의 변경되지 않은 문서는 다시 읽지 않는다.
- 구조·공용 API 변경은 구조 문서의 아키텍처·의존 그래프·feature 경계와 사용부까지 확인한다.
- UI 구현은 UI 가이드의 공통 규칙·해당 확정 편차·완료 점검을 읽고, 가이드가 안내하는 컴포넌트 지도와 프로토타입에서 해당 화면·변형을 확인한다.
- 문서·개발 도구 변경은 계획 단계와 구분하여 요청된 문서와 참조 관계를 확인한다. 불변 조건·예외·의존성이 연결되면 범위를 넓히고, 전체 정합성 점검 시에는 관련 문서 전체를 읽는다.
- `rg -n '^#{1,4} ' <문서>`로 관련 절을 찾고 `rg --files <모듈>`로 실제 파일을 찾는다. 계획의 예정 구조와 현재 구현을 구분한다.
